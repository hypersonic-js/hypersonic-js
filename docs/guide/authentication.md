# Authentication

::: warning Coming soon
This guide is under construction. The content below outlines what will be covered.
:::

This guide covers the Better Auth integration:

- How auth is wired into `createApp` automatically
- Email/password sign-up and sign-in
- Protecting routes with `createAuthGuard`
- Accessing the current user via `req.sessionUser`
- Adding OAuth providers (GitHub, Google) via `hypersonic.config.ts`
- Creating the first admin user with `hypersonic admin create-admin`

For now, refer to the [Quick Start](/guide/quickstart) for the minimal auth configuration.