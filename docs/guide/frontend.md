# Frontend (Inertia + React)

::: warning Coming soon
This guide is under construction. The content below outlines what will be covered.
:::

This guide covers the Inertia + React layer in depth:

- How Inertia bridges Express and React (no REST API required)
- Typing page props with TypeScript interfaces
- Client-side navigation with `<Link>`
- Submitting forms with `useForm`
- Sharing data across all pages with shared props
- The CSRF cookie and how mutations are protected automatically
- Tailwind CSS 4 setup and usage

For now, refer to the frontend setup in the [Quick Start](/guide/quickstart).